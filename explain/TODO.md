## 담당: 최지희

# 최지희 작업 현황 및 남은 일

> 이 파일은 `explain/` 폴더에 있지만, 범위는 **최지희 담당 전체**(`explain/`, `app.py`,
> `ui/`, `data/mock/`)다. 담당 영역이 한 화면 안에서 서로 물려 있어 따로 두면
> 우선순위를 잡을 수 없다. 타 담당에게 보내는 요청 사항은
> `ui/BlueScore_대시보드_설계공유.md`에 있고 여기서 중복하지 않는다.
> 최종 갱신 2026-08-17.

---

## 1. 지금까지 한 일

### (1) 화면 — `app.py`, `ui/` (2026-08-14 커밋 `e7b12df`)

- **어업인 / 심사역 두 페이지로 분리.** 기획서 (8-2)는 두 흐름을 따로 정의하는데
  발표 목업은 한 페이지에 섞여 있었다. 어업인에게 문서번호·해시·승인 버튼은
  의미가 없고, 심사역에게 개선 코칭은 부차적이다.
- **`ui/adapter.py`를 유일한 계산 창구로 고정.** 화면 코드에서는 계산하지 않는다.
  두 화면의 점수가 다르면 "제3자가 관측한 동일한 점수"라는 서비스 전제가 무너진다.
- **상위 % 산출식 오류 발견·수정.** 목업의 `0.65 × A상위% + 0.35 × B상위%`는
  백분위끼리 가중평균한 값이라 통계적으로 성립하지 않는다. 유사군 42척 전체의
  BlueScore를 먼저 내고 그 분포에서 순위를 매기는 방식으로 교체
  (`adapter.top_percent`). 발표 수치가 **상위 27% → 17%** 로 바뀐다.
- **시뮬레이터에 축 간 반작용 반영** (`adapter.simulate`). 목업은 두 슬라이더를
  모두 끝까지 밀면 물리적으로 불가능한 조합이 최고점으로 나왔다. 데모 중 누가
  끝까지 밀면 그 자리에서 드러나는 문제였다.
- **해시를 `CLAUDE.md` 확정 규칙 5번대로 구현** (`adapter.score_hash`).
  목업의 `simpleHash`는 SHA-256이 아니라 화면 시연용 자체 곱셈 함수였다.
- **지도** — Leaflet + Esri World Imagery. pydeck은 베이스맵이 뜨지 않았고
  구글 맵은 결제 등록이 필요해 팀 전원이 못 돌린다. API 키가 필요 없는 조합.
- **판정 불가 화면** (선박 D 표본 부족 · 선박 E 매칭 실패). 기획서 9번
  "모르면 모른다고 한다"를 화면으로 구현.
- **산출 경로 상시 표시** (`components.backend_footer`). 지금 보고 있는 숫자가
  실산출인지 mock 임시값인지 화면 하단에 항상 뜬다.
- **`data/mock/dashboard_mock.json` + 생성 스크립트.** 선박 5척(성공 3 · 보류 2),
  유사군 42척 점수 분포 포함. 화면과 계산이 병렬로 진행되게 하기 위한 것이며
  실산출이 붙는 시점에 폐기한다.

### (2) 설명 계층 — `explain/` (2026-08-14, **아직 미커밋**)

- **설계 변경 — 기여도 계산은 `score/`가, 문장화만 `explain/`이 한다.**
  당초 SHAP 계산까지 이쪽이 하는 것으로 적혀 있었으나, LLM이 숫자를 만들 여지를
  구조적으로 없애려면 계층을 나눠야 한다. `contract.ShapFactor`는 통과만 시킨다.
- `prompt.py` — 계산 결과를 JSON으로 주입하고 문장 생성만 맡기는 프롬프트.
  A축을 "같은 자리를 다시 긁는 주기"로 명시해, LLM이 "같은 어장에 머무르라"는
  정반대 조언을 하는 것을 막았다.
- `render.py` — Strict JSON 파싱 + **숫자 창작 검증**(`find_invented_numbers`).
  응답에 입력에 없던 수치가 섞이면 그 응답을 버리고 폴백으로 강등한다.
- `fallback.py` — LLM 없이도 화면이 비지 않는 템플릿. 한국어 조사(이/가) 처리 포함.
- `facts.py` — RAG 대신 쓰는 고정 사실 테이블. 확정 7건 / 확인 대기 4건.
- `provider.py` + `providers/` — 프로바이더 중립 구조. OpenAI 구현 완료,
  앨런은 스텁.
- `explain.py` — **절대 예외를 던지지 않는다.** 어떤 실패든 폴백으로 흡수하고
  사유를 `source`에 남긴다.
- 화면 연결 — `adapter.explanation()` → 어업인 화면 3번 탭. LLM 생성인지 폴백인지
  화면에 표시(`components.explanation_source`).
- **심사역 화면 연결** — 2번 탭 '산출 근거 요약'이 mock의 고정 문자열
  (`vessel["summary"]`) 대신 `explain/` 생성 문장을 쓴다. 선박당 한 번만 생성해
  캐시하므로 두 화면이 **같은 문장**을 본다 — 화면마다 다른 설명이 나오면
  "제3자가 관측한 동일한 점수"라는 전제가 문장 층에서 깨진다. LLM/폴백 구분도
  함께 표시한다. 심사역에게는 이 구분이 심사 근거의 출처 정보다.
  개선 코칭은 노출 범위표대로 여기 두지 않았다.
- 테스트 27개 통과 (`pytest explain/`).

---

## 2. 확정된 제품 범위 (2026-08-17)

### 시연 페르소나

1. **개선형 어업인** — 현재 BlueScore 우대구간 B인 선박이 개선 시뮬레이터에서
   재방문 횟수·항해속도를 조정하고, A구간 진입 가능성과 예상 금리우대를 확인한다.
2. **이의제기 어업인 + 금융기관 심사역** — 지속가능성 위험 신호로 C구간을 받은
   차주가 점수 근거·데이터 출처·실측/추정 여부를 확인하고 이의제기한다. 심사역은
   리포트와 근거를 검토해 승인/보류를 결정한 뒤 최종 리포트 해시를 온체인에
   커밋하고 기록을 조회한다.

낮은 점수를 고의·불법과 동일시하지 않는다. 화면과 발표에서는 "악성 어업인" 대신
"지속가능성 위험 신호가 높게 관측된 선박" 또는 "C구간 이의제기 차주"라고 표현한다.

### 데이터·AI·체인 범위

- 시연 선박은 실제 선박으로 오인되지 않도록 **실데이터 분포를 반영한 가명·시연용
  선박**으로 고정한다.
- 발표 당일 외부 데이터 API는 호출하지 않는다. 버전이 고정된 로컬 스냅샷을 쓴다.
- 점수·시뮬레이션 숫자는 `score/`가 계산하고, `explain/`은 쉬운 문장으로만 바꾼다.
- RAG는 붙이지 않는다. 요인 코드별 허용 개선팁을 작은 규칙 테이블로 관리한다.
- LLM 설명은 사전 생성·검증·캐시하고, 실시간 호출 실패 시 템플릿 폴백을 쓴다.
- 블록체인은 **온체인 커밋·조회·해시 기록**만 시연한다. 변조 검증 UI는 만들지 않는다.
- 원본 항적·개인정보·이의제기 본문은 온체인에 올리지 않는다.

---

## 3. 구현 순서 및 작업내용

### 1단계 — UI/UX와 시연 시나리오 재설계

- [ ] 어업인 화면 정보구조 재설계: 점수 → 이유 → 개선팁 → 시뮬레이터 → 이의제기.
- [ ] 금융기관 화면 정보구조 재설계: 이의제기 목록 → 상세 근거 → 승인/보류 → 온체인 기록.
- [ ] 페르소나 1(B→A)·페르소나 2(C→이의제기)의 화면별 스토리보드 확정.
- [ ] 모바일 어업인 화면과 데스크톱 심사역 화면의 반응형 기준 확정.
- [ ] "신용등급" 대신 "BlueScore 우대구간" 용어 사용.
- [ ] 실산출·추정·시연용 데이터 배지를 화면 공통 규칙으로 정의.

### 2단계 — 화면 데이터 계약과 시연 데이터 고정

- [ ] `ScoreResponse`, `SimulationResponse`, `ExplanationResponse` 스키마 확정.
- [ ] `AppealCreate`, `AppealDetail`, `ReviewDecision` 스키마 확정.
- [ ] `ChainCommitResponse`, `ChainRecordResponse` 스키마 확정.
- [ ] 모든 결과에 `dataSnapshotId`, `modelVersion`, `scoringRuleVersion`,
      `rateTableVersion`, `sourceType`을 포함.
- [ ] 페르소나 1·2 입력을 별도 fixture로 만들고 화면 결과를 하드코딩하지 않는다.
- [ ] 같은 입력이면 항상 B→A, C구간, 승인/보류 흐름이 재현되는 회귀 테스트 작성.

### 3단계 — SQLite와 서비스 계층

- [ ] SQLite 초기 스키마 작성: `score_runs`, `appeals`, `reviews`, `chain_commits`.
- [ ] 원본 GFW 이벤트는 JSONL.gz/Parquet에 두고 DB에는 경로·스냅샷 메타데이터만 저장.
- [ ] 점수 산출 결과와 당시의 리포트 JSON·SHA-256 해시 저장.
- [ ] 어업인 이의제기 제출 → 금융기관 목록 조회 → 승인/보류 상태 전이 구현.
- [ ] 앱 재시작 후에도 이의제기·심사·온체인 기록이 복원되는지 검증.
- [ ] 시연 초기 상태를 반복 생성하는 seed/reset 스크립트 마련.

### 4단계 — 실데이터·모델 어댑터 연결

- [ ] `ui/adapter.scoring_backend()`의 오래된 "조업 이벤트 데이터 미확보" 분기 제거.
- [ ] 실제 GFW 스냅샷 → A축 → 유사군 → `raw_to_score()` 연결.
- [ ] `score/score_assembly.py`와 `score/rate_mapping.py`를 정식 계산 경로로 사용.
- [ ] B축은 실산출/추정/시연값 상태를 명시하고 검증되지 않은 부분을 숨기지 않는다.
- [ ] 시뮬레이터의 UI 고정상수를 `score/tradeoff_coefficients.py` 결과로 교체하거나,
      미연결 시 정책 시뮬레이션 예시임을 명시.
- [ ] 매칭 신뢰도·실측/추정·결측 사유를 응답에 포함.

### 5단계 — FastAPI 구현

- [ ] `GET /vessels`, `GET /vessels/{id}/score`.
- [ ] `POST /vessels/{id}/simulate`, `GET /vessels/{id}/explanation`.
- [ ] `POST /appeals`, `GET /appeals`, `GET /appeals/{id}`.
- [ ] `POST /appeals/{id}/review`.
- [ ] `POST /reports/{id}/commit`, `GET /chain/records/{recordId}`.
- [ ] 데이터·모델·LLM·체인 오류를 화면용 상태코드로 변환.
- [ ] 데이터 수집 API 키는 FastAPI/프론트에 두지 않고 수집 환경에서만 관리.

### 6단계 — 프론트엔드 연결

- [ ] `dashboard_mock.json` 직접 참조를 제거하고 FastAPI 응답으로 교체.
- [ ] 페르소나 1: 현재 점수·근거·개선팁·B→A 시뮬레이션 연결.
- [ ] 페르소나 2: C구간 상세 설명·이의제기 작성·제출 완료 상태 연결.
- [ ] 금융기관: 이의제기 목록·상세 리포트·승인/보류 및 사유 저장 연결.
- [ ] 점수 미산출·표본 부족·매칭 실패·API 실패 화면 유지.
- [ ] 어업인/심사역이 같은 `score_run_id`와 리포트를 보도록 보장.

### 7단계 — `explain/` 실제 결과 연결

- [ ] 실제 `score/` 요인 라벨을 `ShapFactor` 및 개선팁 코드와 매핑.
- [ ] 점수 엔진이 확정한 숫자·시뮬레이션 결과만 `ExplainInput`에 전달.
- [ ] 요인 코드별 `allowedRecommendations` 규칙 테이블 작성.
- [ ] LLM이 허용 목록 밖의 행동·수치·법률 판단을 생성하지 못하게 프롬프트 강화.
- [ ] `OPENAI_API_KEY`가 있는 개발 환경에서 실제 호출 품질과 숫자 검증 통과율 확인.
- [ ] 페르소나 1·2 설명을 사전 생성해 SQLite 또는 fixture에 캐시.
- [ ] LLM 실패·숫자 검증 실패 시 현재 템플릿 폴백 유지.
- [ ] `facts.PENDING_FACTS`는 1차 출처 확인 후에만 확정 사실로 이동.

### 8단계 — 온체인 커밋·조회 연결

- [ ] UI 해시 중복 구현을 `chain.hashing.compute_result_hash()` 호출로 교체.
- [ ] 최종 심사 리포트 payload에 점수·결정·스냅샷·모델·산식·규칙표 버전 포함.
- [ ] 승인/보류 후 `HashRegistry.commit()` 호출.
- [ ] 트랜잭션 해시·블록번호·기록시각·컨트랙트 주소를 `chain_commits`에 저장.
- [ ] 심사역 화면에서 Record ID로 `HashRegistry.get()` 결과 조회.
- [ ] 변조 전/후 비교·검증 UI는 구현 범위에서 제외.

### 9단계 — 통합 검증과 발표 리허설

- [ ] 페르소나 1 전체 흐름을 네트워크 없이 반복 실행.
- [ ] 페르소나 2의 어업인→금융기관 상태 전달을 별도 화면/세션에서 확인.
- [ ] 앱·API 재시작 후 SQLite 기록 복원 확인.
- [ ] LLM 호출 실패 시 폴백으로 발표가 계속되는지 확인.
- [ ] 로컬 Hardhat 노드·컨트랙트 사전 배포 및 연결 상태 확인.
- [ ] 발표용 seed 상태 복원 절차와 실패 시 대체 화면 준비.

---

## 4. 우선순위

### P0 — 시연 성립에 필수

1. UI/UX·시나리오와 응답 스키마 확정.
2. SQLite 이의제기·심사 상태 저장.
3. 실제 A축·점수조립·금리매핑 연결.
4. FastAPI 및 두 화면 연결.
5. 검증된 LLM 설명·폴백 연결.
6. 최종 리포트 온체인 커밋·조회.

### P1 — 완성도 향상

1. 시뮬레이터 점수 곡선과 축 간 상충효과 시각화.
2. 격자 재방문 히트맵.
3. 어업인 모바일 레이아웃 정리.
4. 데이터 출처·매칭 신뢰도·실측/추정 상세 보기.

### 이번 범위에서 만들지 않을 것

- RAG·벡터 DB·임베딩 검색.
- 음성·챗봇 인터페이스.
- 발표 당일 외부 데이터 API 실시간 호출.
- 블록체인 변조 검증 UI.
- 개인정보·항적 원본의 온체인 저장.

---

## 5. 설계 원칙

- **FastAPI는 프론트엔드가 아니라 애플리케이션 백엔드다.** 프론트는 확정된 API
  계약만 소비하고 점수·해시를 직접 계산하지 않는다.
- **SQLite는 마지막 부가기능이 아니다.** 이의제기·심사·온체인 기록을 잇는 업무
  상태의 기준 저장소이므로 API 구현 전에 스키마를 고정한다.
- **LLM은 숫자와 개선효과를 만들지 않는다.** `score/`가 만든 사실과 허용된 개선팁을
  어업인이 이해할 수 있는 문장으로 바꾸는 역할만 한다.
- **블록체인은 DB를 대체하지 않는다.** 상세 리포트와 이의제기·심사 이력은 SQLite에,
  최종 리포트 해시와 기록시각은 온체인에 둔다.
- **시연 결과는 결정론적이어야 한다.** 외부 데이터·LLM 호출 장애가 있어도 고정
  스냅샷과 검증된 캐시·폴백으로 두 시나리오가 끝까지 진행돼야 한다.
