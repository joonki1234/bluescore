# 작성: 오동규 (김태윤님께)

# 다음 작업 제안 (2026-08-14)

지금까지 진행 상황 정리하고, `VESSEL_SPEC_API_KEY` 쿼터 다 쓰신 상태에서
그 키 없이 지금 바로 할 수 있는 작업들 우선순위로 정리했습니다.

## 1. 지금까지 진행 상황

| 항목 | 상태 |
|---|---|
| GFW (선박+이벤트) | **완료** — 어선 31,605척, 이벤트 913,715건 |
| 국내 선박제원정보 — 후보 수집 | **방금 완료** — 대상 9,468척 전체 처리, 3,917척 스킵(콜사인/이름 둘 다 없음), 실패 0건. (원래 63.6%에서 멈춰있던 걸 제가 이어받아 끝까지 돌렸습니다.) |
| 국내 선박제원정보 — 실제 매칭 판정 | **미실행** — 새 후보 데이터 기준으로 `match_vessel_spec.py` 재실행 필요 (아래 2번) |
| TAC 어종별 소진현황 + 할당승인정보 | **완료** |
| 어업별어선(업종별 총마력) | 파일만 확보, 로더/활용 코드 없음 |
| 해양수산부 AIS 위치정보 통계(2020년, 77만 행) | 파일만 확보, 로더/활용 코드 없음 |
| 해양기상 | API 연결·필드 검증 완료, 대량 수집·이벤트 부착은 미착수 |
| 연안AIS 통계 API | 활용신청 완료, 승인 대기 (외부 요인) |
| 국내→GFW 어업종 매핑(19개 유형) | 미착수, 담당 미정 |

## 2. `VESSEL_SPEC_API_KEY` 없이 지금 바로 가능한 작업 (우선순위 순)

### 2-1. `match_vessel_spec.py` 재실행 — API 호출 없음, 로컬 파일만 사용
방금 후보 수집이 9,468척 전체(기존 63.6% → 100%) 기준으로 끝났습니다.
이걸 반영해서 매칭 결과를 다시 뽑아야 실제 커버리지가 늘어납니다.
(제가 로컬에서 커밋까지는 해뒀는데 push는 오동규가 직접 하기로 해서,
아직 원격엔 안 올라가 있을 수 있습니다 — `git pull`로 최신 후보 데이터
받은 뒤 실행해주세요.)

### 2-2. `match_tac_vessels.py` 재실행 — API 호출 없음
2-1 결과가 바뀌니 TAC 매칭도 같이 갱신됩니다.

### 2-3. 해양수산부 AIS 위치정보 통계 로더 작성 — API 호출 없음, 이미 받아둔 파일
`data/raw/해양수산부_AIS 위치정보 통계_20201027.TXT` (해구번호·일자·일시·척수·
좌상단/우하단 경도위도, 77만 행). 기획서에서 "혼잡가중압력의 기준값"으로
쓰라고 명시한 데이터입니다. 지금 `score/axis_a_pressure.py`의 혼잡압력이
GFW 이벤트 개수만으로 어림잡는 상태라, 이 공식 통계로 대체/보강하면
정확도가 크게 오를 수 있습니다. EUC-KR 인코딩이라 그대로 읽으면 깨지니
주의 필요합니다.

### 2-4. 어업별어선 CSV 로더 작성 — API 호출 없음, 이미 받아둔 파일
`data/raw/해양수산부_수산정보_MR 어업별어선_20240910.csv`. 업종별 총마력을
엔진출력 보정값으로 쓰라고 기획서에 명시된 파일인데, 아직 아무 코드도 안
읽고 있습니다.

### 2-5. (선택) 해양기상 대량 수집·이벤트 부착
`MARINE_WEATHER_API_KEY`는 `VESSEL_SPEC_API_KEY`와 별도 쿼터라 아직
쓸 수 있을 가능성이 높습니다(확인 필요). 원래 "선박 매칭 끝나면 진행"
계획이셨는데, 방금 후보 수집이 끝났으니 타이밍이 맞습니다.

## 3. 담당 범위 확인 부탁드립니다

- **`score/` 폴더는 `ROLES.md`에 김준기·오동규 공동 담당으로 돼 있습니다.**
  `axis_a_pressure.py`, `axis_b_physics.py`, `axis_b_baseline.py` 관련
  로직 작업은 저희 둘이 맡을 테니, 김태윤님은 `data/` 쪽 원본 확보·정제·
  매칭에 집중해주시면 됩니다. (지금까지도 그렇게 해오셨고, 그대로 유지
  하자는 확인 차원입니다.)
- **국내→GFW 어업종 매핑(19개 유형)**은 데이터 수집 문제가 아니라 도메인
  판단이 필요한 작업이라 김태윤님 혼자 결정하지 마시고, 초안만 만들어서
  팀 회의에서 확정하는 걸 권장드립니다.
