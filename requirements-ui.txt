# 담당: 최지희
#
# 대시보드(app.py)만 실행할 때 필요한 최소 패키지.
# 화면은 계산 모듈을 직접 임포트하지 않고 FastAPI REST 응답만 사용한다.
#
#   pip install -r requirements-ui.txt
#   streamlit run app.py
#
# A축·B축 실산출까지 돌려보려면 requirements.txt(전체)를 설치해야 한다.
streamlit
plotly
python-dotenv
openai
requests
